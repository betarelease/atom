'use strict';

var React = require('react');

var ConfigItemTitle = React.createClass({
  displayName: 'ConfigItemTitle',

  render: function render() {
    var nodes = [React.createElement(
      'span',
      { key: this.props.name + 'title' },
      this.props.title
    )];
    if (this.props.required) {
      nodes.push(React.createElement(
        'span',
        { key: this.props.name + 'required' },
        ' ',
        React.createElement(
          'small',
          { className: 'text-muted' },
          '(Required)'
        )
      ));
    } else if (this.props.recommended) {
      nodes.push(React.createElement(
        'span',
        { key: this.props.name + 'recommended' },
        ' ',
        React.createElement(
          'small',
          { className: 'text-muted' },
          '(Recommended)'
        )
      ));
    }
    return React.createElement(
      'span',
      null,
      nodes
    );
  }
});

module.exports = ConfigItemTitle;
//# sourceMappingURL=ConfigItemTitle.js.map
